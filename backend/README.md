# GenomeLens API

Modular FastAPI service for validated educational DNA analysis. Install `requirements.txt`, run `uvicorn app.main:app --reload`, and open `/docs` for request and response schemas. Configure exact browser origins with `ALLOWED_ORIGINS`. Run `pytest` from this directory.
