const sections=[['Accepted input','Paste raw DNA, paste FASTA, or upload .fasta, .fa, and .txt files up to 2 MB. Analyses accept up to 1,000,000 cleaned bases.'],['Cleaning & validation','Whitespace and line breaks are removed, letters are uppercased, and FASTA header lines beginning with > are excluded. A, T, G, C, and N are accepted; all invalid characters are reported.'],['Coordinates','All displayed sequence positions are one-based and inclusive. Reverse-strand ORFs are mapped back to coordinates on the submitted forward sequence.'],['Translation & ORFs','GenomeLens uses NCBI standard genetic code 1. Ambiguous codons translate to X. Incomplete trailing codons are ignored. Candidate ORFs begin at ATG and extend to the first in-frame TAA, TAG, or TGA; boundary-reaching ORFs are marked as lacking a stop.'],['Molecular weight','The estimate sums average nucleotide residue masses and subtracts water released by phosphodiester bond formation. N uses an averaged residue mass. It is an educational approximation.'],['Melting temperature','Sequences under 14 known bases use the Wallace rule (2°C per A/T and 4°C per G/C). Longer sequences use Biopython’s GC-based empirical method (valueset 7). N bases are excluded. Salt and experimental conditions can materially alter real Tm.'],['Complexity','Shannon entropy measures base diversity. Ambiguous percentage reports N frequency. The longest homopolymer is the longest consecutive run of a single base.'],['Privacy','Sequences are sent to the configured API, processed in memory, and not permanently stored by default. Avoid submitting sensitive or identifiable genomic data to deployments you do not trust.'],['Limitations','No similarity search, annotation database, quality scores, circular-DNA handling, alternative genetic codes, or laboratory validation is performed. Results cannot identify a gene or establish function.']];
export default function Documentation(){return <div className="container content-page docs"><header className="page-hero"><div className="eyebrow"><span/>Methods & documentation</div><h1>Transparent by <em>design.</em></h1><p>Every result should be understandable, reproducible, and interpreted within its limits.</p><a className="button primary" href={`${import.meta.env.VITE_API_URL||'http://localhost:8000'}/docs`} target="_blank" rel="noreferrer">Open interactive API docs ↗</a></header><div className="doc-layout"><aside>{sections.map((x,i)=><a key={x[0]} href={`#method-${i}`}>{x[0]}</a>)}</aside><div>{sections.map((x,i)=><section id={`method-${i}`} key={x[0]}><span>0{i+1}</span><h2>{x[0]}</h2><p>{x[1]}</p></section>)}</div></div></div>}
