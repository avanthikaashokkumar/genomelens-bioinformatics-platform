import {useState} from 'react'; import {NavLink} from 'react-router-dom'; import {Dna, Menu, X} from 'lucide-react';
export default function Navbar(){const [open,setOpen]=useState(false);return <header className="navbar"><NavLink className="brand" to="/" onClick={()=>setOpen(false)}><span className="brand-mark"><Dna size={21}/></span><span>GenomeLens</span></NavLink><button className="menu" aria-label="Toggle navigation" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button><nav className={open?'open':''} aria-label="Primary navigation">{[['/','Home'],['/lab','DNA Lab'],['/learn','Learn'],['/docs','Docs'],['/about','About']].map(([to,label])=><NavLink key={to} to={to} onClick={()=>setOpen(false)}>{label}</NavLink>)}</nav></header>}
